public enum Rotation {
  North = 0,
  East = 90,
  South = 180,
  West = 270
}

public enum ObjectTypes {
  Building,
  Ground
}

public enum Sprites {
  Grass,
  GrassDark,
  Red,
  Red2,
  Street,
  White
}

public enum InteractionModes {
  Interact,
  Build,
  Destroy
}